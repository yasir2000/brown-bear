# Gerrit Code Review
server {
    listen 80;
    listen 443 ssl http2;
    server_name gerrit.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://gerrit;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60;
        proxy_send_timeout 60;
        proxy_read_timeout 60;
    }
}

# Grafana Monitoring Dashboard
server {
    listen 80;
    listen 443 ssl http2;
    server_name grafana.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://grafana;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support for live updates
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# LDAP Admin Interface
server {
    listen 80;
    listen 443 ssl http2;
    server_name ldap.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://ldap-admin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# MailHog (Development Mail Testing)
server {
    listen 80;
    listen 443 ssl http2;
    server_name mail.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://mailhog;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support for real-time mail updates
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
