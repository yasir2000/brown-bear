# Jenkins CI/CD
server {
    listen 80;
    listen 443 ssl http2;
    server_name jenkins.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://jenkins;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Jenkins specific configuration
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Server $host;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_connect_timeout 150;
        proxy_send_timeout 100;
        proxy_read_timeout 100;
        proxy_buffers 4 32k;
        client_max_body_size 8m;
        client_body_buffer_size 128k;
    }
}

# SonarQube Code Quality
server {
    listen 80;
    listen 443 ssl http2;
    server_name sonar.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    location / {
        proxy_pass http://sonarqube;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60;
        proxy_send_timeout 60;
        proxy_read_timeout 60;
    }
}

# Nexus Repository Manager
server {
    listen 80;
    listen 443 ssl http2;
    server_name nexus.brownbear.local;

    ssl_certificate /etc/nginx/ssl/brownbear.local.crt;
    ssl_certificate_key /etc/nginx/ssl/brownbear.local.key;

    # Redirect HTTP to HTTPS
    if ($scheme = http) {
        return 301 https://$server_name$request_uri;
    }

    client_max_body_size 1G;

    location / {
        proxy_pass http://nexus;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}
